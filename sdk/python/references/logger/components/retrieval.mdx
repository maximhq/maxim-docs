# maxim.logger.components.retrieval

## [RetrievalConfig](/sdk/python/references/logger/components/retrieval)

[View source on GitHub](https://github.com/maximhq/maxim-py/blob/main/maxim/logger/components/retrieval.py#L17)


```python
@deprecated(
    "This class will be removed in a future version. Use {} which is TypedDict."
)

@dataclass
class RetrievalConfig()
```

[Retrieval](/sdk/python/references/logger/components/retrieval) config.

This class provides functionality to manage retrieval configurations.

## [RetrievalConfigDict](/sdk/python/references/logger/components/retrieval)

[View source on GitHub](https://github.com/maximhq/maxim-py/blob/main/maxim/logger/components/retrieval.py#L28)


```python
class RetrievalConfigDict(TypedDict)
```

[Retrieval](/sdk/python/references/logger/components/retrieval) config dict.

This class provides functionality to manage retrieval config dictionaries.

#### get\_retrieval\_config\_dict

[View source on GitHub](https://github.com/maximhq/maxim-py/blob/main/maxim/logger/components/retrieval.py#L39)


```python
def get_retrieval_config_dict(
        config: Union[RetrievalConfig, RetrievalConfigDict]) -> dict[str, Any]
```

Get the retrieval config dict.

**Arguments**:

- `config` _Union[[RetrievalConfig](/sdk/python/references/logger/components/retrieval), [RetrievalConfigDict](/sdk/python/references/logger/components/retrieval)]_ - The config to get the dict from.
  

**Returns**:

  dict[str, Any]: The retrieval config dict.

## [Retrieval](/sdk/python/references/logger/components/retrieval)

[View source on GitHub](https://github.com/maximhq/maxim-py/blob/main/maxim/logger/components/retrieval.py#L63)


```python
class Retrieval(BaseContainer)
```

[Retrieval](/sdk/python/references/logger/components/retrieval).

This class represents a retrieval.

#### \_\_init\_\_

[View source on GitHub](https://github.com/maximhq/maxim-py/blob/main/maxim/logger/components/retrieval.py#L69)


```python
def __init__(config: Union[RetrievalConfig, RetrievalConfigDict],
             writer: LogWriter)
```

Initialize a retrieval.

**Arguments**:

- `config` - The config to initialize the retrieval with.
- `writer` - The writer to use.

#### input

[View source on GitHub](https://github.com/maximhq/maxim-py/blob/main/maxim/logger/components/retrieval.py#L83)


```python
def input(query: str)
```

Set the input for the retrieval.

**Arguments**:

- `query` - The query to set for the retrieval.

#### input\_

[View source on GitHub](https://github.com/maximhq/maxim-py/blob/main/maxim/logger/components/retrieval.py#L96)


```python
@staticmethod
def input_(writer: LogWriter, id: str, query: str)
```

Set the input for the retrieval.

**Arguments**:

- `writer` - The writer to use.
- `id` - The id of the retrieval.
- `query` - The query to set for the retrieval.

#### output

[View source on GitHub](https://github.com/maximhq/maxim-py/blob/main/maxim/logger/components/retrieval.py#L107)


```python
def output(docs: Union[str, List[str]])
```

Set the output for the retrieval.

**Arguments**:

- `docs` - The docs to set for the retrieval.

#### add\_attachment

[View source on GitHub](https://github.com/maximhq/maxim-py/blob/main/maxim/logger/components/retrieval.py#L121)


```python
def add_attachment(attachment: Union[FileAttachment, FileDataAttachment,
                                     UrlAttachment])
```

Add an attachment to the retrieval.

**Arguments**:

- `attachment` - The attachment to add to the retrieval.

#### add\_attachment\_

[View source on GitHub](https://github.com/maximhq/maxim-py/blob/main/maxim/logger/components/retrieval.py#L133)


```python
@staticmethod
def add_attachment_(writer: LogWriter, id: str,
                    attachment: Union[FileAttachment, FileDataAttachment,
                                      UrlAttachment])
```

Add an attachment to the retrieval.

**Arguments**:

- `writer` - The writer to use.
- `id` - The id of the retrieval.
- `attachment` - The attachment to add to the retrieval.

#### output\_

[View source on GitHub](https://github.com/maximhq/maxim-py/blob/main/maxim/logger/components/retrieval.py#L151)


```python
@staticmethod
def output_(writer: LogWriter, id: str, docs: Union[str, List[str]])
```

Set the output for the retrieval.

**Arguments**:

- `writer` - The writer to use.
- `id` - The id of the retrieval.
- `docs` - The docs to set for the retrieval.

#### end\_

[View source on GitHub](https://github.com/maximhq/maxim-py/blob/main/maxim/logger/components/retrieval.py#L169)


```python
@staticmethod
def end_(writer: LogWriter, id: str, data: Optional[Dict[str, Any]] = None)
```

End the retrieval.

**Arguments**:

- `writer` - The writer to use.
- `id` - The id of the retrieval.
- `data` - The data to set for the retrieval.

#### add\_tag\_

[View source on GitHub](https://github.com/maximhq/maxim-py/blob/main/maxim/logger/components/retrieval.py#L191)


```python
@staticmethod
def add_tag_(writer: LogWriter, id: str, key: str, value: str)
```

Add a tag to the retrieval.

**Arguments**:

- `writer` - The writer to use.
- `id` - The id of the retrieval.
- `key` - The key of the tag.
- `value` - The value of the tag.

